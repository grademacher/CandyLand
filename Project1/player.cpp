#include "player.h"
#include "gameboard.h"


Player::Player(GameBoard* this_board)
{
  board = this_board;

}


Player::~Player()
{
}

bool Player::MovePlayer() {
  //draw a new card
  //determine what spot to move to
  //update the current tile to reflect the current spot
  //update the gameboard to reflect the player moving

  return true;
}
